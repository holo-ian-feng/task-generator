# Test Report — 2026-06-11 09:27:59

## Summary

| Result  | Count |
|---------|-------|
| Passed  | 147   |
| Failed  | 0     |
| Errors  | 0     |
| XFailed | 13    |
| XPassed | 0     |

**Overall: PASS**
Duration: 3.24s

> PASS when failed=0 and xpassed=0.

---

## Known Defects (XFailed)

These tests document confirmed bugs. They are expected to fail until each defect is fixed.
When fixed, the test will show as XPASS — remove the `xfail` marker at that point.

| Test | Reason |
|------|--------|
| `tests/integration/test_docker_compose_prod.py::TestHealthChecks::test_temporal_healthcheck_is_not_trivially_true` | temporal healthcheck is `['CMD', 'true']` — always passes immediately, making it a non-functional health gate. Replace with a real TCP/gRPC check. |
| `tests/regression/test_config_defects.py::test_empty_service_role_key_raises_at_startup` | DEFECT #6a: Settings accepts empty string for SUPABASE_SERVICE_ROLE_KEY. Worker starts then silently fails every Supabase call with 401. Fix: add pydantic field_validator. |
| `tests/regression/test_config_defects.py::test_empty_azure_ai_key_raises_at_startup` | DEFECT #6b: Settings accepts empty string for AZURE_AI_KEY. Worker starts but every AI call fails at runtime. Fix: add pydantic field_validator. |
| `tests/regression/test_config_defects.py::test_temporal_address_without_port_raises_at_startup` | DEFECT #9: TEMPORAL_ADDRESS without port causes gRPC on port 443 with no useful error. Fix: validate `:` in address at startup. |
| `tests/regression/test_deployment_defects.py::test_migration_step_no_silent_pipe_suppression` | DEFECT #1: Migration step uses `\| true` (pipe) not `\|\| true` (logical OR). All az failures silently produce exit 0. Migrations never run. |
| `tests/regression/test_deployment_defects.py::test_migration_job_is_explicitly_started` | DEFECT #2: `az containerapp job start` never called. Migration job registered with Manual trigger but never fired. |
| `tests/regression/test_deployment_defects.py::test_secret_bearing_update_fallback_includes_env_vars[supabase-db]` | DEFECT #3: `az containerapp update` for supabase-db omits `--env-vars`. Secret rotation won't propagate. |
| `tests/regression/test_deployment_defects.py::test_secret_bearing_update_fallback_includes_env_vars[supabase-auth]` | DEFECT #3: `az containerapp update` for supabase-auth omits `--env-vars`. Secret rotation won't propagate. |
| `tests/regression/test_deployment_defects.py::test_secret_bearing_update_fallback_includes_env_vars[supabase-rest]` | DEFECT #3: `az containerapp update` for supabase-rest omits `--env-vars`. Secret rotation won't propagate. |
| `tests/regression/test_deployment_defects.py::test_secret_bearing_update_fallback_includes_env_vars[temporal-worker]` | DEFECT #3: `az containerapp update` for temporal-worker omits `--env-vars`. Secret rotation won't propagate. |
| `tests/regression/test_deployment_defects.py::test_pgrst_db_schemas_consistent_across_environments` | DEFECT #5: PGRST_DB_SCHEMAS is `public,storage,graphql_public` in compose but only `public` in deploy.yml. Storage and GraphQL fail in production. |
| `tests/regression/test_deployment_defects.py::test_db_migrate_does_not_use_restart_on_failure` | DEFECT #8: db-migrate uses `restart: on-failure` with non-idempotent SQL. Partial migrations cause infinite restart loop. |
| `tests/unit/test_kong_config.py::TestCorsPolicy::test_wildcard_origin_not_combined_with_credentials` | `credentials: true` + `origins: ['*']` is rejected by all browsers. Fix: specify explicit origin list. |

---

*Generated by `/run-tests` skill*
